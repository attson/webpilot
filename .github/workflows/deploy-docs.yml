name: Deploy Docs Site

on:
  push:
    branches:
      - main
    paths:
      - "docs-site/**"
      # The demo is built into the site, so a change to it alone must still
      # redeploy — otherwise the homepage silently keeps the old demo.
      - "packages/extension/demo/**"
      - "packages/extension/vite.demo.config.ts"
      - "packages/shared/src/llm/builtin-tool-defs.ts"
      - ".github/workflows/deploy-docs.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    name: Build VitePress site
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Enable pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      - name: Install docs-site deps
        working-directory: docs-site
        run: pnpm install --frozen-lockfile --ignore-workspace

      - name: Generate tools reference
        working-directory: docs-site
        run: pnpm gen

      - name: Install workspace deps
        run: pnpm install --frozen-lockfile

      - name: Build embedded demo
        # Writes into docs-site/public/demo, which VitePress copies verbatim —
        # so this has to run before the site build.
        run: pnpm -F @atwebpilot/extension build:demo

      - name: Build site
        working-directory: docs-site
        run: pnpm exec vitepress build

      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs-site/.vitepress/dist

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
